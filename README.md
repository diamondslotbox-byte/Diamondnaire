# Diamondnaire

This is the Diamondnaire project.

## Installation

1. Clone the repository
2. Run `npm install` to install dependencies
3. Create a `.env` file from the `.env.example` file.
4. Run `npm start` to start the server.
